(()=>{const addHead=(tag,attrs)=>{const el=document.createElement(tag);Object.entries(attrs).forEach(([k,v])=>el.setAttribute(k,v));document.head.appendChild(el)};if(!document.querySelector('link[rel="manifest"]'))addHead('link',{rel:'manifest',href:'/static/manifest.webmanifest'});if(!document.querySelector('meta[name="theme-color"]'))addHead('meta',{name:'theme-color',content:'#f26a2e'});if(!document.querySelector('meta[name="apple-mobile-web-app-capable"]'))addHead('meta',{name:'apple-mobile-web-app-capable',content:'yes'});if(!document.querySelector('meta[name="apple-mobile-web-app-status-bar-style"]'))addHead('meta',{name:'apple-mobile-web-app-status-bar-style',content:'default'});if(!document.querySelector('link[rel="apple-touch-icon"]'))addHead('link',{rel:'apple-touch-icon',href:'/static/icons/icon-192.svg'});
let deferredInstall=null;window.addEventListener('beforeinstallprompt',event=>{event.preventDefault();deferredInstall=event;renderPwaPanel()});
const standalone=()=>window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;
const isiOS=()=>/iphone|ipad|ipod/i.test(navigator.userAgent);
const supported=()=>('serviceWorker'in navigator)&&('Notification'in window)&&('PushManager'in window);
const b64ToBytes=value=>{const pad='='.repeat((4-value.length%4)%4),base64=(value+pad).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(base64);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))};
async function registerWorker(){if(!('serviceWorker'in navigator))return null;return navigator.serviceWorker.register('/static/sw.js',{scope:'/'}).then(()=>navigator.serviceWorker.ready)}
async function subscribe(){if(!supported()){showMessage('Push notifications are not supported on this device.');return}if(isiOS()&&!standalone()){showMessage('On iPhone, add GlucoPlate to the Home Screen first, then open the installed app and enable notifications.');return}const permission=await Notification.requestPermission();if(permission!=='granted'){showMessage('Notification permission was not granted.');return}const registration=await registerWorker();const config=await fetch('/api/push/config').then(r=>r.json());if(!config.vapid_public_key){showMessage('Push is ready in the app, but the server VAPID key is not configured yet.');return}let subscription=await registration.pushManager.getSubscription();if(!subscription)subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64ToBytes(config.vapid_public_key)});const session=JSON.parse(localStorage.getItem('glucoplate_session')||'null');await fetch('/api/push/subscriptions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({subscription:subscription.toJSON(),user_id:session?.user?.user_id||null,profile_id:session?.active_profile?.profile_id||null})});if('setAppBadge'in navigator)navigator.setAppBadge(1).catch(()=>{});showMessage('Phone notifications are enabled.');renderPwaPanel()}
async function install(){if(deferredInstall){deferredInstall.prompt();await deferredInstall.userChoice;deferredInstall=null;renderPwaPanel();return}if(isiOS()&&!standalone())showMessage('Tap Share in Safari, choose Add to Home Screen, then open GlucoPlate from its new icon.');else showMessage('Use your browser menu to install GlucoPlate.')}
function showMessage(message){if(typeof window.toast==='function')window.toast(message);else{let node=document.getElementById('pwaMessage');if(!node){node=document.createElement('div');node.id='pwaMessage';node.style='position:fixed;left:50%;bottom:88px;transform:translateX(-50%);z-index:999;background:#211f1d;color:white;padding:11px 15px;border-radius:999px;max-width:90%;text-align:center;font:600 13px system-ui';document.body.appendChild(node)}node.textContent=message;clearTimeout(node._timer);node._timer=setTimeout(()=>node.remove(),5000)}}
function panelHtml(){const notificationText=Notification?.permission==='granted'?'Notifications enabled':'Enable notifications';return `<div id="pwaPanel" style="margin-top:16px;padding:16px;border:1px solid #e7ded4;border-radius:18px;background:#fff"><strong style="display:block">Install & phone notifications</strong><span style="display:block;color:#756f69;font-size:.82rem;margin:5px 0 12px">Install GlucoPlate for an app-like experience. iPhone push requires the Home Screen app.</span><div style="display:flex;gap:8px;flex-wrap:wrap"><button id="installPwaBtn" class="btn secondary" type="button">${standalone()?'App installed':'Install app'}</button><button id="enablePushBtn" class="btn primary" type="button">${notificationText}</button></div></div>`}
function renderPwaPanel(){document.getElementById('pwaPanel')?.remove();const profile=document.getElementById('profileView');const target=profile?.querySelector('.card')||document.querySelector('main')||document.body;target.insertAdjacentHTML('beforeend',panelHtml());document.getElementById('installPwaBtn').onclick=install;document.getElementById('enablePushBtn').onclick=subscribe}
window.addEventListener('DOMContentLoaded',()=>{registerWorker().catch(()=>{});renderPwaPanel()});window.GlucoPlatePwa={install,subscribe,registerWorker}})();