const CACHE='glucoplate-shell-v9';
const SHELL=['/static/index.html','/static/login.html','/static/register.html','/static/manifest.webmanifest','/static/device-manager.js','/static/native-cook.js','/static/native-timers.js','/static/native-ingredients.js','/static/offline-actions.js','/static/firebase-auth.js','/static/firebase-user-data.js','/static/profile-personalization.js','/static/pantry-ui.js','/static/recommendation-ui.js','/static/native-pwa.css','/static/pwa.js','/static/icons/icon-192.svg','/static/icons/icon-512.svg'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting()))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin!==self.location.origin)return;event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();if(url.pathname.startsWith('/static/'))caches.open(CACHE).then(cache=>cache.put(event.request,copy));return response}).catch(()=>caches.match(event.request).then(hit=>hit||caches.match('/static/index.html'))))});
self.addEventListener('push',event=>{let payload={};try{payload=event.data?event.data.json():{}}catch{payload={body:event.data?.text()||'GlucoPlate has an update for you.'}}const data={...(payload.notification||{}),...(payload.data||{}),...payload};const title=data.title||'GlucoPlate AI';const options={body:data.body||'Your kitchen update is ready.',icon:'/static/icons/icon-192.svg',badge:'/static/icons/icon-192.svg',tag:data.tag||'glucoplate-update',renotify:Boolean(data.renotify),data:{url:data.url||data.link||'/static/index.html'},actions:data.actions||[]};event.waitUntil(self.registration.showNotification(title,options))});
self.addEventListener('notificationclick',event=>{event.notification.close();const target=event.notification.data?.url||'/static/index.html';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const client of list){if('focus'in client){client.navigate(target);return client.focus()}}return clients.openWindow(target)}))});