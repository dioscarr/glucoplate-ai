# v0.5 Authentication

## Goal

Move GlucoPlate from browser-local usage toward real user accounts and cloud-backed personalization.

## Status

Functionally complete except Apple sign-in, which is blocked until an Apple Developer account is available.

## User outcomes

- Users can sign in with Google or email/password.
- Saved recipes follow the user across devices.
- Notification tokens are tied to authenticated users.
- Profiles support family or household preferences.
- Active-profile preferences automatically shape recipe generation.
- Enterprise users are enrolled by access code and appear in the administrator directory.

## Tasks

- [x] Add Firebase Authentication client setup.
- [x] Add backend Firebase ID token verification.
- [x] Add authenticated user records backed by Firebase Realtime Database.
- [x] Store saved recipes under authenticated Firebase user IDs.
- [x] Associate FCM tokens with authenticated user IDs.
- [x] Add profile selection or household profile support.
- [x] Apply active-profile preferences to recipe generation.
- [x] Add sign-out and basic account UI.
- [x] Document auth environment variables.
- [ ] Add Apple sign-in — blocked: Apple Developer account required.
- [x] Add email/password account flow.
- [x] Add enterprise access-code enrollment and custom claims.
- [x] Synchronize authenticated enterprise sessions into the enterprise directory.
- [x] Add role-protected enterprise administrator user listing.

## Testing

- [x] Anonymous user foundation.
- [x] Google sign-in foundation.
- [ ] Apple sign-in flow — blocked: Apple Developer account required.
- [x] Email/password account foundation.
- [x] Expired or invalid token behavior.
- [x] Enterprise session membership synchronization.
- [x] Saved recipe ownership integration coverage.
- [x] Notification token ownership.
- [x] End-to-end administrator dashboard smoke test.
- [x] Household profile preference and cooking-history isolation.
- [x] Active-profile recipe personalization contract.

## Implementation notes

Firebase Authentication is the identity provider. Recipe data remains under `app_data/enterprises/{enterprise_id}/users/{firebase_uid}`, while administrator membership data is maintained under `enterprise_directory`. The Firebase session endpoint performs an idempotent directory upsert so existing authenticated users are backfilled on their next session.

Saved-recipe integration tests verify that recipe listing and deletion are scoped by both enterprise ID and Firebase UID, and that accounts without enterprise membership cannot access enterprise recipe storage.

FCM registration, test delivery, and token deletion require a Firebase bearer token. The backend derives token ownership from the verified Firebase UID rather than accepting a client-supplied user ID.

Administrator dashboard smoke coverage verifies synchronized member retrieval, role-based denial for regular members, enterprise-membership enforcement, and the static dashboard's user rendering and empty-state contract.

Household profiles are stored under `app_data/enterprises/{enterprise_id}/users/{firebase_uid}/profiles/{profile_id}`. Each profile has independent preferences and cooking history, while the existing account-level saved recipe collection remains shared by the household. The profile page provides profile creation and active-profile selection.

Before recipe generation, the client loads the active profile and its preferences, then merges allergies and dislikes into `avoid_ingredients`, dietary choices into generation preferences, preferred cuisines into style guidance, and the profile's serving-size default into the request. Generated recipes display the active profile name so the personalization context remains visible.

## Release notes draft

GlucoPlate supports Firebase sessions with Google and email/password sign-in, cloud-backed recipes, authenticated notifications, enterprise administration, household profiles, and profile-aware recipe generation. Apple sign-in remains blocked until an Apple Developer account is available.
